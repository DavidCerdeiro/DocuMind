spring:
  application:
    name: documind-test
  docker:
    compose:
      enabled: false
  
  ai:
    vectorstore:
      pgvector:
        initialize-schema: true
        index-type: HNSW

app:
    document:
      chunk-size: 500
      chunk-overlap: 40